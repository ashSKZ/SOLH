const {
  generarEstadoHospitalario,
  reiniciarSimulacion,
  obtenerEstadoActual
} = require("./services/simulacionService");

module.exports = {
  generarEstadoHospitalario,
  reiniciarSimulacion,
  obtenerEstadoActual
};
